#pragma once

// ============================================================
// CREDENCIALES DEL NODO (¡NO COMMITEAR!)
// ============================================================
// Copiar este archivo como `firmware/include/secrets.h` (ya está en .gitignore)
// y rellenar con las credenciales reales de la cuenta "nodo" del proyecto
// Firebase `medidor-de-humedad`.
//
// IMPORTANTE (rotación): la cuenta `nodo@medidor.cl` se expuso en el repo.
// Cambiar la contraseña de esa cuenta en Firebase Console (Authentication ->
// Users -> nodo@medidor.cl -> Change password) y poner aquí la nueva.
//
// El email/contraseña se usan solo en `cloud.cpp` (signInWithPassword) para
// obtener el ID token con el que el nodo publica telemetría. Sin este archivo,
// el firmware compila con valores vacíos y la publicación falla en runtime.

#define FIREBASE_AUTH_EMAIL "nodo@medidor.cl"
#define FIREBASE_AUTH_PASSWORD "CAMBIAR-ESTA-CONTRASENA"